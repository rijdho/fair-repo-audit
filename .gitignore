.DS_Store
node_modules/
.wrangler/

# Per-machine tool permissions, not project config. Was covered only by a global
# ignore file, which does not travel with a clone — so it lives here too.
.claude/settings.local.json
